import React from 'react';
import '../App.css';

const Rules = () => {
    return (
        <div className="rules">
            <h3>Select a picture to gain a point.  Click each picture once and you win, but if you select the same picture twice you lose!</h3>
        </div>
    );
}

export default Rules;